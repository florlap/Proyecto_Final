import { Link } from "react-router-dom";

export default function LandingPage(props) {
    return (
        <div>
        <Link to="/login">Login</Link>
        <h1> Landing page</h1>
      </div>
    );
}
